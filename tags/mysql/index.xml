<?xml version="1.0" encoding="utf-8" standalone="yes" ?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>Mysql on Typical personal blog</title>
    <link>https://benetis.me/tags/mysql/index.xml</link>
    <description>Recent content in Mysql on Typical personal blog</description>
    <generator>Hugo -- gohugo.io</generator>
    <language>en-us</language>
    <copyright>All rights reserved - 2017</copyright>
    <atom:link href="https://benetis.me/tags/mysql/index.xml" rel="self" type="application/rss+xml" />
    
    <item>
      <title>Real estate ads scraping with Scala and Akka #1 - Proof of concept</title>
      <link>https://benetis.me/post/real-estate-prices/</link>
      <pubDate>Sun, 06 Aug 2017 18:54:24 +0200</pubDate>
      
      <guid>https://benetis.me/post/real-estate-prices/</guid>
      <description>

&lt;h2 id=&#34;introduction&#34;&gt;Introduction&lt;/h2&gt;

&lt;p&gt;Disclaimer: Not an expert in scala world - any feedback is appreciated.&lt;/p&gt;

&lt;p&gt;A question came to mind - can I crawl through a real estate website, gather some data and infer something from that data. This is also becoming relevant and interesting to me.&lt;/p&gt;

&lt;p&gt;What experience I want to get doing this side project:&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;Data scraping (common problems, &amp;hellip;)&lt;/li&gt;
&lt;li&gt;Concurrency while analyzing, gathering data (Akka Aktors)&lt;/li&gt;
&lt;li&gt;Data analysis, statistical inference (Knowing what to query and what results mean)&lt;/li&gt;
&lt;li&gt;Data visualization (how to display data in a meaningful way)&lt;/li&gt;
&lt;li&gt;DevOps experience (long running app: deploy, updates, backups, &amp;hellip;)&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&#34;planning-phase&#34;&gt;Planning phase&lt;/h2&gt;

&lt;p&gt;How I imagine this little spider would work:&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;Spin up scala service&lt;/li&gt;
&lt;li&gt;Start Supervisor with instructions to crawl specific category of real estate (example: apartment/flat rents)&lt;/li&gt;
&lt;li&gt;This Supervisor spawns some spiders for different RT (real estate) websites&lt;/li&gt;
&lt;li&gt;Spider visits website, gets raw data, mines it, passes it to store&lt;/li&gt;
&lt;li&gt;Store saves it into database&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;&lt;img src=&#34;https://benetis.me/images/2017/07/rt-prices-plan.png&#34; alt=&#34;&#34; /&gt;&lt;/p&gt;

&lt;p&gt;Surely we could easily expand this to support multiple websites, but for the sake of simplicity will stick to one.&lt;/p&gt;

&lt;p&gt;Talking about technology stack.&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;Scala, Akka - a language I admire
&lt;del&gt;- InfluxDB - Interesting database for monitoring real time data - want to try out&lt;/del&gt;&lt;/li&gt;
&lt;li&gt;MySQL - battle tested DB, our scraper will not &amp;ldquo;scale&amp;rdquo; that much to start encountering lock problems&lt;/li&gt;
&lt;li&gt;&lt;del&gt;Docker - I want application to run smoothly and automated. I have some prior experience with it - might just use it&lt;/del&gt; Can&amp;rsquo;t use it due that fact that 32bit platforms are not supported by docker engine. You might ask, but why 32bit? Its just that I got this old laptop laying around which is begging to be resurrected.&lt;/li&gt;
&lt;/ul&gt;

&lt;h4 id=&#34;legal-corner&#34;&gt;Legal corner&lt;/h4&gt;

&lt;p&gt;Before scraping websites I advise to read their ToS on their policy of scraping. Unless you are doing it for commercial purposes - owners will usually be okay with it. Be sure to be polite to the website (not hit it with a lot of requests). Web is an open space - unless you &amp;ldquo;accidentally DOS&amp;rdquo; or steal content you will be fine.&lt;/p&gt;

&lt;p&gt;Website to &amp;ldquo;mine&amp;rdquo; on - &lt;a href=&#34;aruodas.lt&#34;&gt;aruodas.lt&lt;/a&gt; - popular real estate website in Lithuania. Surely I am not planning to share mined raw data with anyone, sell it or keep for a long time. After contacting their administration they were nice enough to give me a permit (on my IP address) to scrape their ads. Thanks a lot!&lt;/p&gt;

&lt;h2 id=&#34;choice-of-database&#34;&gt;Choice of database&lt;/h2&gt;

&lt;p&gt;Initial plan was to try out influxdb, grafana. While I certainly did that - I encountered some tooling problems, which I though will be easier to work around. Especially with influxdb. Tools are lacking to view data, CLI tools are not that good to view data or interact with it. Sure it works well with data monitoring of real time data, but it is not something I am looking for. Chronograf at this moment also doesn&amp;rsquo;t seem to fit my use case. I bet for monitoring CPU it would be perfect, but for &amp;ldquo;long term&amp;rdquo; storage data - not worth the trouble. That being said - let&amp;rsquo;s grab what we know will work - MySQL or this expirment will never get finished&lt;/p&gt;

&lt;h2 id=&#34;getting-stuff-done&#34;&gt;Getting stuff done&lt;/h2&gt;

&lt;p&gt;Let&amp;rsquo;s start talking about classifier&lt;/p&gt;

&lt;p&gt;Here you can find a 1000 meters view of app&amp;rsquo;s architecture. You have r estate categories, real estate sites.&lt;/p&gt;

&lt;p&gt;Categories classify different types of ads and sites distinguish between different sites we are scraping. Each category might have different implementation + each site for sure will have different implementation for how scrapers need to behave.&lt;/p&gt;

&lt;p&gt;&lt;img src=&#34;https://benetis.me/images/2017/08/rt-categories-sites.png&#34; alt=&#34;&#34; /&gt;&lt;/p&gt;

&lt;p&gt;Also we have ad itself and its properties. As we are scraping only one website now - we will base it on it and extend as we go. Might happen that we need different ones for different categories, but no reason to overengineer at this moment.&lt;/p&gt;

&lt;p&gt;RTDetails is just some sugar to have types for different ad properties. Not sure if this is a good pattern at this moment - not going to go into detail. (pun not intended)&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-language-scala&#34;&gt;package rt

trait RTCategory {
}

trait RTSite {
  def categoryId(category: RTCategory): String
}

case class RTFlatsRent() extends RTCategory

case class RTFlatsSell() extends RTCategory

case class RTHousesSell() extends RTCategory

case class RTAruodas() extends RTSite {

  override def categoryId(category: RTCategory): String = category match {
    case RTFlatsRent() =&amp;gt; &amp;quot;4&amp;quot;
    case RTFlatsSell() =&amp;gt; &amp;quot;1&amp;quot;
    case RTHousesSell() =&amp;gt; &amp;quot;2&amp;quot;
  }

}


case class RTItem(
                        itemId: Option[String],
                        url: Option[String],
                        price: Option[Double],
                        pricePerMeter: Option[Double],
                        area: Option[Double],
                        rooms: Option[Int],
                        floor: Option[Int],
                        numberOfFloors: Option[Int],
                        buildYear: Option[Int],
                        houseType: Option[String],
                        heatingSystem: Option[String],
                        equipment: Option[String],
                        shortDescription: Option[String],
                        comment: Option[String],
                        created: Option[String],
                        edited: Option[String],
                        interested: Option[String]
                      )

case class RTDetailsArea(value: Double) {
}

case class RTDetailsNumberOfRooms(value: Int)

case class RTDetailsFloor(value: Int)

case class RTDetailsNumberOfFloors(value: Int)

case class RTDetailsBuildYear(value: Int)

case class RTDetailsHouseType(value: String)

case class RTDetailsHeatingSystem(value: String)

case class RTDetailsEquipment(value: String)

case class RTDetailsShortDescription(value: String)
&lt;/code&gt;&lt;/pre&gt;

&lt;h3 id=&#34;next-up-scraper-and-supervisor&#34;&gt;Next up - Scraper and Supervisor&lt;/h3&gt;

&lt;p&gt;Two more things to talk about: scraper and supervisor&lt;/p&gt;

&lt;p&gt;Scraper is pretty straightforward. Receive a message from Actor and start scraping it. Once we get what we need - pass message to store it into database. Nothing more at this moment. More complex composition/abstraction will need to land here later to fit in more sites and specific categories.&lt;/p&gt;

&lt;p&gt;One thing I don&amp;rsquo;t like about this &amp;ldquo;MVP&amp;rdquo; implementation is how parsedItemDetails are parsed with collectFirst by types. This makes my IDE slow due type checking. Some better data structure can be fit here - not to waste too much time here - let&amp;rsquo;s move on.&lt;/p&gt;

&lt;p&gt;p.s scraping done with the help of &amp;ldquo;scalascraper&amp;rdquo;&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-scala&#34;&gt;package rt

import akka.actor.{Actor, ActorRef}
import net.ruippeixotog.scalascraper.browser.JsoupBrowser
import net.ruippeixotog.scalascraper.dsl.DSL.Extract._
import net.ruippeixotog.scalascraper.dsl.DSL._
import net.ruippeixotog.scalascraper.model.Element
import net.ruippeixotog.scalascraper.scraper.ContentExtractors.element

import scala.util.Try

class Scraper(supervisor: ActorRef) extends Actor {

  override def receive: Receive = {
    case ScrapDetails(url, rtSite, rtCategory) =&amp;gt;
      println(&amp;quot;=== start scraping details ===&amp;quot;)
      println(s&amp;quot;url = $url&amp;quot;)
      sender() ! StoreDetails(scrapDetails(url, rtSite, rtCategory), rtSite, rtCategory)
  }

  def scrapDetails(url: String,
                   rTSite: RTSite,
                   rTCategory: RTCategory): RTItem = {
    val browser = new JsoupBrowser(
      &amp;quot;Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/58.0.3029.110 Safari/537.36&amp;quot;)

    val doc = browser.get(url)

    println(&amp;quot;=== [DETAILS] FLATS ===&amp;quot;)
    val id = url.substring(url.length - 10, url.length - 1)

    val priceEleOpt = doc &amp;gt;?&amp;gt; element(&amp;quot;.obj-price&amp;quot;)

    priceEleOpt match { // if page exists
      case Some(priceEle) =&amp;gt;
        val (price, pricePerMeter) = parsePrice(priceEle)

        val comment = doc &amp;gt;&amp;gt; element(&amp;quot;.obj-comment&amp;quot;) &amp;gt;?&amp;gt; text

        val detailsTerms = doc &amp;gt;&amp;gt; elementList(&amp;quot;.obj-details dt&amp;quot;)
        val detailsItem = doc &amp;gt;&amp;gt; elementList(&amp;quot;.obj-details dd&amp;quot;)

        val itemDetails = detailsTerms.zip(detailsItem)

        lazy val parsedItemDetails: Seq[Any] = this.splitDetails(itemDetails)

        val stats = doc &amp;gt;&amp;gt; elementList(&amp;quot;.obj-stats dl dd&amp;quot;)

        val created = stats(1).text
        val edited = stats(2).text
        val interested = stats(3).text


        RTItem(
          Some(id)
          , Some(url)
          , price
          , pricePerMeter
          , parsedItemDetails.collectFirst { case d: RTDetailsArea =&amp;gt; d.value }
          , parsedItemDetails.collectFirst { case d: RTDetailsNumberOfRooms =&amp;gt; d.value }
          , parsedItemDetails.collectFirst { case d: RTDetailsFloor =&amp;gt; d.value }
          , parsedItemDetails.collectFirst { case d: RTDetailsNumberOfFloors =&amp;gt; d.value }
          , parsedItemDetails.collectFirst { case d: RTDetailsBuildYear =&amp;gt; d.value }
          , parsedItemDetails.collectFirst { case d: RTDetailsHouseType =&amp;gt; d.value }
          , parsedItemDetails.collectFirst { case d: RTDetailsHeatingSystem =&amp;gt; d.value }
          , parsedItemDetails.collectFirst { case d: RTDetailsEquipment =&amp;gt; d.value }
          , parsedItemDetails.collectFirst { case d: RTDetailsShortDescription =&amp;gt; d.value }
          , comment
          , Some(created)
          , Some(edited)
          , Some(interested)
        )
      case None =&amp;gt; RTItem(Some(id), None, None, None, None, None, None, None, None, None, None, None, None, None, None, None, None)
    }


  }

  private def parsePrice(price: =&amp;gt; Element)
  : (Option[Double], Option[Double]) = {

    def extractPricePerMeter(priceWithoutAdvert: Option[String]): Option[Double] = {

      val capturePricePerMeterWithWhiteSpace = &amp;quot;\\([0-9 ]+&amp;quot;.r

      val matched = capturePricePerMeterWithWhiteSpace
        .findFirstIn(priceWithoutAdvert.getOrElse(&amp;quot;-1&amp;quot;))

      matched
        .map(_.replace(&amp;quot; &amp;quot;, &amp;quot;&amp;quot;))
        .map(_.replace(&amp;quot;(&amp;quot;, &amp;quot;&amp;quot;))
        .map(_.toDouble)

    }

    def extractPriceWithoutCurrency(priceWithoutAdvert: Option[String]): Option[Double] = {
      val capturePriceWithoutCurrencyWithWhiteSpace = &amp;quot;^\\s?[0-9 ]+&amp;quot;.r

      val matched = capturePriceWithoutCurrencyWithWhiteSpace
        .findFirstIn(priceWithoutAdvert.getOrElse(&amp;quot;-1&amp;quot;))

      matched
        .map(_.replace(&amp;quot; &amp;quot;, &amp;quot;&amp;quot;))
        .map(_.toDouble)
    }

    def extractPriceWithoutAdvert: Option[String] = {
      val priceAdvert: Option[String] = price &amp;gt;?&amp;gt; element(&amp;quot;.price-change&amp;quot;) match {
        case Some(ele: Element) =&amp;gt; ele &amp;gt;?&amp;gt; text
        case None =&amp;gt; None
      }

      val rawPriceOpt = price &amp;gt;?&amp;gt; text

      rawPriceOpt match {
        case Some(rawPrice: String) =&amp;gt; priceAdvert match {
          case Some(advert: String) =&amp;gt; Some(rawPrice.replace(advert, &amp;quot;&amp;quot;))
          case None =&amp;gt; Some(rawPrice)
        }
        case None =&amp;gt; None
      }
    }

    val priceWithoutAdvert = extractPriceWithoutAdvert

    val pricePerMeter = extractPricePerMeter(priceWithoutAdvert)

    val priceWithoutCurrency = extractPriceWithoutCurrency(priceWithoutAdvert)

    (
      priceWithoutCurrency,
      pricePerMeter
    )
  }

  private def splitDetails(details: Seq[(Element, Element)]): Seq[Any]

  = {
    lazy val _details = details.map({
      case (term, item) =&amp;gt; term.text match {
        case &amp;quot;Area (m²):&amp;quot; =&amp;gt; convertAreaToDouble(item.text)
        case &amp;quot;Number of rooms :&amp;quot; =&amp;gt; RTDetailsNumberOfRooms(item.text.toInt)
        case &amp;quot;Floor:&amp;quot; =&amp;gt; RTDetailsFloor(item.text.toInt)
        case &amp;quot;No. of floors:&amp;quot; =&amp;gt; RTDetailsNumberOfRooms(item.text.toInt)
        case &amp;quot;Build year:&amp;quot; =&amp;gt; RTDetailsBuildYear(item.text.toInt)
        case &amp;quot;House Type:&amp;quot; =&amp;gt; RTDetailsHouseType(item.text)
        case &amp;quot;Heating system:&amp;quot; =&amp;gt; RTDetailsHeatingSystem(item.text)
        case &amp;quot;Equipment:&amp;quot; =&amp;gt; RTDetailsEquipment(item.text)
        case &amp;quot;Description:&amp;quot; =&amp;gt; RTDetailsShortDescription(item.text)
        case _ =&amp;gt; 1.0d
      }
    })

    def convertAreaToDouble(area: String): Option[RTDetailsArea] = {
      Some({
        val areaStr = area
          .dropRight(3) // drop m^2
          .replace(&amp;quot;,&amp;quot;, &amp;quot;.&amp;quot;) //to dots notation

        if (areaStr.isEmpty)
          RTDetailsArea(0.0d)
        else
          RTDetailsArea(areaStr.toDouble)
      })
    }

    _details
  }

}

&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Last piece of our proof concept - Supervisor.&lt;/p&gt;

&lt;p&gt;Here you can inspect &amp;ldquo;communication&amp;rdquo; channels for our actors. Implementation is pretty straightforward without any edge case handling. Moreover, you have to handle ids yourself which kinda sucks, but we can improve on that later.&lt;/p&gt;

&lt;p&gt;You can also inspect 2 second delay on our scraper - this is not to cause any performance problems for the website.&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-scala&#34;&gt;package rt

import akka.actor.{Actor, ActorRef, ActorSystem, _}

import scala.concurrent.ExecutionContext.Implicits.global
import scala.concurrent.duration._

case class StartDetails(rtSite: RTSite,
                        rTCategory: RTCategory)

case class StoreDetails(details: RTItem,
                        rtSite: RTSite,
                        rTCategory: RTCategory)

case class EndDetails()

case class ScrapDetails(url: String,
                        rtSite: RTSite,
                        rTCategory: RTCategory)

class Supervisor(system: ActorSystem) extends Actor {

  var host2Actor = Map.empty[String, ActorRef]

  val scrapers = system.actorOf(Props(new Scraper(self)))

  override def receive: Receive = {
    case StartDetails(rtSite: RTSite, rTCategory: RTCategory) =&amp;gt; startDetails(rtSite, rTCategory)
    case StoreDetails(details: RTItem, rtSite: RTSite, rtCategory: RTCategory) =&amp;gt;
      storeDetails(details, rtSite, rtCategory)
    case EndDetails =&amp;gt; ???
  }

  private def storeDetails(details: RTItem,
                           rtSite: RTSite,
                           rtCategory: RTCategory) = {
    Store.writeDetails(details, rtSite, rtCategory)
  }

  private def startDetails(site: RTSite,
                           category: RTCategory) = {

    var id = 2284843
    val last = 2271641
    system.scheduler.schedule(2 seconds, 2 seconds)({
      scrapers ! ScrapDetails(
        s&amp;quot;https://en.aruodas.lt/1-$id/&amp;quot;,
        site,
        category
      )
      id = id + 1
    })

  }


}

&lt;/code&gt;&lt;/pre&gt;

&lt;h3 id=&#34;deployment-step&#34;&gt;&amp;ldquo;Deployment&amp;rdquo; step&lt;/h3&gt;

&lt;p&gt;Next - preparing for &amp;ldquo;deployment&amp;rdquo;. We will be putting into a an old laptop behind the tv, in private home network. We need to generate a JAR file with all dependencies. For that - &lt;code&gt;sbt-assembly&lt;/code&gt; comes to help. Was a bit tough to set it up - but seems to work well.&lt;/p&gt;

&lt;p&gt;Configure mysql users.&lt;/p&gt;

&lt;p&gt;More on how to - &lt;a href=&#34;https://github.com/sbt/sbt-assembly&#34;&gt;https://github.com/sbt/sbt-assembly&lt;/a&gt;&lt;/p&gt;

&lt;p&gt;Copy everything to the server with &lt;code&gt;scp&lt;/code&gt; and run &lt;code&gt;java -jar rt-pricer-assembly.jar&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;You can find code here - &lt;a href=&#34;https://github.com/benetis/rt-pricer&#34;&gt;https://github.com/benetis/rt-pricer&lt;/a&gt;&lt;/p&gt;

&lt;p&gt;That&amp;rsquo;s it!&lt;/p&gt;

&lt;h3 id=&#34;results&#34;&gt;Results&lt;/h3&gt;

&lt;p&gt;A gif how new rows appear in database after code with this specific revision.&lt;/p&gt;

&lt;p&gt;&lt;img src=&#34;https://benetis.me/images/2017/08/rt-pricer-rows-adding.gif&#34; alt=&#34;&#34; /&gt;&lt;/p&gt;

&lt;p&gt;&lt;a href=&#34;https://github.com/benetis/rt-pricer/tree/7b1f783ea94aca5bcaa65e5dc8cb97ff2320e0de&#34;&gt;Code revision&lt;/a&gt;&lt;/p&gt;

&lt;h3 id=&#34;feedback&#34;&gt;Feedback&lt;/h3&gt;

&lt;p&gt;If you have any suggestions - I am eagerly waiting for feedback. &lt;a href=&#34;https://benetis.me/post/contact-me/&#34;&gt;https://benetis.me/post/contact-me/&lt;/a&gt;&lt;/p&gt;
</description>
    </item>
    
  </channel>
</rss>